[build]
  command = "cd frontend && npm install && CI=false npm run build"
  publish = "frontend/build"

[[redirects]]
  from = "/api/*"
  to = "/api/index.py"
  status = 200

[[redirects]]
  from = "/*"
  to = "/client/:splat"
  status = 200
